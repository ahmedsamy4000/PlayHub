import 'package:flutter/material.dart';

class PlaygroundReservationsScreen extends StatelessWidget {
  const PlaygroundReservationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}