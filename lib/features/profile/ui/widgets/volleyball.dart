import 'package:flutter/material.dart';

class VolleyballTab extends StatelessWidget {
  const VolleyballTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('You have no bookings yet.'),);
  }
}