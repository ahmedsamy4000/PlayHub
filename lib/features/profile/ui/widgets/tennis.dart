import 'package:flutter/material.dart';

class TennisTab extends StatelessWidget {
  const TennisTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('You have no bookings yet.'),);
  }
}